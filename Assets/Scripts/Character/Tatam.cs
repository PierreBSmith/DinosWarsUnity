using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Tatam : Character
{
    // Start is called before the first frame update
    void Awake()
    {
        this.moveRange = 5;
        this.speed = 3;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

}
